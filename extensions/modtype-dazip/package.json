{
  "name": "modtype-dazip",
  "version": "1.0.0",
  "description": "Support for dazip (Dragon Age) mods",
  "scripts": {
    "_build": "node build.mjs && pnpm extractInfo",
    "build": "pnpm run _build && node ../copy-extension.mjs out",
    "dist": "pnpm run _build && node ../copy-extension.mjs dist"
  },
  "author": "Black Tree Gaming Ltd.",
  "license": "GPL-3.0",
  "type": "commonjs",
  "private": true,
  "config": {
    "extensionName": "Mod Type: dazip (Dragon Age)"
  },
  "devDependencies": {
    "@types/redux": "catalog:",
    "@types/semver": "catalog:",
    "bluebird": "catalog:",
    "copyfiles": "catalog:",
    "i18next": "catalog:",
    "semver": "catalog:",
    "vortex-api": "workspace:*"
  }
}
