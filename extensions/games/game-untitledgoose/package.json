{
  "name": "game-untitledgoose",
  "version": "1.0.0",
  "description": "Support for Untitled Goose Game",
  "scripts": {
    "_assets": "copyfiles -u 1 -f ./assets/* dist",
    "_build": "node build.mjs && pnpm run _assets && pnpm extractInfo",
    "build": "pnpm run _build && node ../../copy-extension.mjs out",
    "dist": "pnpm run _build && node ../../copy-extension.mjs dist"
  },
  "author": "Black Tree Gaming Ltd.",
  "license": "GPL-3.0",
  "type": "commonjs",
  "private": true,
  "config": {
    "game": "Untitled Goose Game"
  },
  "devDependencies": {
    "vortex-api": "workspace:*"
  }
}
