{
  "name": "game-mount-and-blade2",
  "version": "0.5.0",
  "description": "This is a stub of the Mount and Blade II: Bannerlord extension. It will be downloaded the first time it's managed.",
  "main": "dist/index.js",
  "scripts": {
    "_assets": "copyfiles -u 1 -f ./assets/* dist",
    "_build": "copyfiles -u 1 -f ./src/*.js dist && pnpm run _assets && pnpm extractInfo",
    "build": "pnpm run _build && node ../../copy-extension.mjs out",
    "dist": "pnpm run _build && node ../../copy-extension.mjs dist"
  },
  "author": "Black Tree Gaming Ltd.",
  "license": "GPL-3.0",
  "type": "commonjs",
  "private": true,
  "config": {
    "game": "Mount and Blade II: Bannerlord"
  },
  "devDependencies": {
    "vortex-api": "workspace:*"
  }
}
