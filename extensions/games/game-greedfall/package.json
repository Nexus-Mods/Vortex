{
  "name": "game-greedfall",
  "version": "1.0.2",
  "description": "Support for Greedfall",
  "scripts": {
    "build": "node build.mjs",
    "test": "pnpm exec vitest run"
  },
  "author": "Black Tree Gaming Ltd.",
  "license": "GPL-3.0",
  "type": "commonjs",
  "private": true,
  "config": {
    "game": "Greedfall"
  },
  "dependencies": {
    "exe-version": "workspace:*"
  },
  "devDependencies": {
    "@nexusmods/vortex-api": "workspace:*",
    "@vortex/extension-test-mocks": "workspace:*"
  },
  "nx": {
    "tags": [
      "vortex:extension"
    ],
    "targets": {
      "build": {
        "inputs": [
          "default",
          "typescript",
          "vortex-api"
        ]
      }
    }
  }
}
