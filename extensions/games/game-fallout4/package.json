{
  "name": "game-fallout4",
  "version": "1.0.7",
  "description": "Support for Fallout 4",
  "main": "dist/index.js",
  "scripts": {
    "_assets": "copyfiles -u 1 -f ./assets/*.jpg ./assets/*.png dist",
    "_build": "copyfiles -u 1 -f ./src/*.js dist && pnpm run _assets && extractInfo",
    "build": "pnpm run _build",
    "dist": "pnpm run _build"
  },
  "author": "Black Tree Gaming Ltd.",
  "license": "GPL-3.0",
  "type": "commonjs",
  "private": true,
  "config": {
    "game": "Fallout 4"
  },
  "devDependencies": {
    "vortex-api": "catalog:"
  }
}
