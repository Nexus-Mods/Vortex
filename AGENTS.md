# Agent Instructions

Use `pnpm run` for repo commands. After code changes, run `build`, `test`, `lint`, and `format` on the affected package.

## Conditional Context

- For repo navigation or code search: read `AGENTS-DIRECTORIES.md`
- For testing: read `AGENTS-TESTING.md`
- For frontend/UI work (React/TypeScript renderer): read `AGENTS-FRONTEND.md`
- For packaging, installers, or the release pipeline: read `AGENTS-PACKAGING.md`
- For debugging in VS Code (breakpoints, launch configs): read `AGENTS-DEBUGGING.md`
- For collections or phased installation: read `AGENTS-COLLECTIONS.md`
- For the auto-updater (update flow, channels, downgrades): read `docs/updater.md`
- For testing updates locally (mock feed, test builds, env vars): read `docs/updater-testing.md`
