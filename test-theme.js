const { BrowserWindow } = require("electron"); const windows = BrowserWindow.getAllWindows(); if (windows.length > 0) { windows[0].webContents.executeJavaScript(`console.log("Current theme:", window.vortexApi?.store?.getState()?.settings?.interface?.currentTheme); window.vortexApi?.events?.emit("apply-theme", "compact"); setTimeout(() => console.log("Theme after switch:", window.vortexApi?.store?.getState()?.settings?.interface?.currentTheme), 1000);`); }
