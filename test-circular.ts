import { IState } from './src/types/IState';
import { IExtensionApi } from './src/types/IExtensionContext';

// Just importing these types to see if it causes the stack overflow
console.log('Testing circular dependencies');