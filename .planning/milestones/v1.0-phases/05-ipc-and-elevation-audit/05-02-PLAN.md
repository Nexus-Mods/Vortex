---
phase: 05-ipc-and-elevation-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md
autonomous: true
requirements:
  - IPC-04

must_haves:
  truths:
    - "The audit document answers whether runElevated() is called on any startup path (answer: no)"
    - "The audit document answers whether pkexec is needed for Phase 1 (answer: no)"
    - "All runElevated call sites are enumerated with file paths and line numbers"
  artifacts:
    - path: ".planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md"
      provides: "Elevation scope audit for Phase 1"
      contains: "runElevated"
  key_links: []
---

<objective>
Write the IPC-04 elevation audit document that confirms `runElevated()` is not on any startup code path and pkexec is not needed for Phase 1.

Purpose: Closes the last open question from the Phase 1 milestone — whether Linux needs an elevation mechanism (pkexec) to boot. The answer is no: all elevation call sites are user-triggered mod deployment operations.

Output: One audit document in the phase directory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ipc-and-elevation-audit/05-CONTEXT.md
@.planning/phases/05-ipc-and-elevation-audit/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write elevation audit document</name>
  <files>.planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md</files>
  <read_first>
    - src/renderer/src/util/elevated.ts (full file — confirm runElevated export and elevatedMain structure)
    - src/renderer/src/util/fs.ts (lines 1060-1090 — confirm elevated() helper is user-triggered file ops)
    - src/renderer/src/ExtensionManager.ts (lines 2640-2670 — confirm runElevated call is user-triggered custom tools)
    - src/renderer/src/extensions/symlink_activator_elevate/index.ts (lines 550-570, 790-810, 920-940, 1050-1060 — confirm all four calls are mod deployment operations)
    - src/main/src/main.ts (confirm no runElevated on startup path)
    - src/main/src/Application.ts (confirm no runElevated on startup path)
    - src/renderer/src/renderer.tsx (confirm no runElevated on startup path)
  </read_first>
  <action>
    Per D-04: Create `.planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md` with the following structure and content:

    Title: `# Elevation Audit — Phase 1 Scope`

    Section 1 — `## Summary`: State that `runElevated()` is NOT called on any startup code path. pkexec is NOT needed for Phase 1. All elevation call sites are user-triggered mod deployment operations.

    Section 2 — `## runElevated Call Sites`: A table with columns: File, Line, Context, Trigger. Include these six confirmed call sites:
    1. `src/renderer/src/util/fs.ts` — line 1078 — `elevated()` helper for file copy/move ops — User-triggered (mod deployment file operations)
    2. `src/renderer/src/ExtensionManager.ts` — line 2659 — `runElevated()` via `startIPC()` — User-triggered (elevated custom tools)
    3. `src/renderer/src/extensions/symlink_activator_elevate/index.ts` — line 566 — symlink deployment — User-triggered (mod deployment)
    4. Same file — line 806 — symlink deployment — User-triggered (mod deployment)
    5. Same file — line 930 — symlink deployment — User-triggered (mod deployment)
    6. Same file — line 1057 — symlink deployment — User-triggered (mod deployment)

    Section 3 — `## Startup Path Analysis`: Confirm that these startup entry points do NOT contain or transitively call `runElevated()`:
    - `src/main/src/main.ts` — Electron main process bootstrap
    - `src/main/src/Application.ts` — Application lifecycle
    - `src/renderer/src/renderer.tsx` — React renderer bootstrap
    State that `runElevated` is only imported in three renderer-side files, and none of those imports are executed during startup initialization.

    Section 4 — `## Phase 1 Elevation Decision`: State: "pkexec is NOT required for Phase 1. The `winapi.ShellExecuteEx` shim from Phase 2 (WAPI-04) already throws a clear error on Linux when elevation is attempted. Since `runElevated()` is never called during startup, this throw is acceptable — it only fires if a user explicitly triggers an elevated operation (mod deployment)."

    Section 5 — `## Future Work (v2)`: Reference ELEV-01 and ELEV-02 from REQUIREMENTS.md. Note that full pkexec + polkit integration is deferred to v2 and will be needed when Linux users attempt symlink-based mod deployment.

    Section 6 — `## Methodology`: "Audit performed by grep search for `runElevated` across `src/renderer/src/` and manual inspection of each call site's execution context. Startup entry points (`main.ts`, `Application.ts`, `renderer.tsx`) inspected for transitive calls."

    Footer: `*Audit date: {current date}*` and `*Phase: 05-ipc-and-elevation-audit*`
  </action>
  <verify>
    <automated>test -f .planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md && grep -c "runElevated" .planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md | xargs test 5 -le</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md` exists
    - File contains `# Elevation Audit`
    - File contains `## runElevated Call Sites` with a table listing all six call sites
    - File contains `src/renderer/src/util/fs.ts` and `line 1078`
    - File contains `src/renderer/src/ExtensionManager.ts` and `line 2659`
    - File contains `symlink_activator_elevate/index.ts` and lines `566`, `806`, `930`, `1057`
    - File contains `## Startup Path Analysis` with confirmation that main.ts, Application.ts, renderer.tsx do NOT call runElevated
    - File contains `pkexec is NOT required for Phase 1` (or equivalent clear statement)
    - File contains `## Future Work` referencing ELEV-01 and ELEV-02
  </acceptance_criteria>
  <done>Elevation audit document exists, enumerates all six runElevated call sites, confirms none are on the startup path, and states pkexec is not needed for Phase 1</done>
</task>

</tasks>

<verification>
1. `test -f .planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md` — file exists
2. `grep "pkexec is NOT" .planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md` — contains the key conclusion
3. `grep -c "runElevated" .planning/phases/05-ipc-and-elevation-audit/05-ELEVATION-AUDIT.md` — at least 6 mentions (one per call site)
</verification>

<success_criteria>
- Elevation audit document exists at the specified path
- All six runElevated call sites are enumerated with file, line, and trigger context
- Document explicitly answers both IPC-04 questions: (1) not on startup path, (2) pkexec not needed for Phase 1
- Future work section points to v2 requirements
</success_criteria>

<output>
After completion, create `.planning/phases/05-ipc-and-elevation-audit/05-02-SUMMARY.md`
</output>
