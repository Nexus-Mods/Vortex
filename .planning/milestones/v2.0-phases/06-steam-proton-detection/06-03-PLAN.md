---
phase: 06-steam-proton-detection
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - extensions/games/game-fallout4/src/index.js
autonomous: false
requirements: [STAM-05]

must_haves:
  truths:
    - "Fallout 4 extension loads without MODULE_NOT_FOUND on Linux"
    - "Cyberpunk 2077 extension has no winapi-bindings dependency"
    - "Stardew Valley extension has native Linux executable detection"
    - "Skyrim SE inherits STAM-04 fix — INI paths resolve to Wine prefix on Linux"
  artifacts:
    - path: "extensions/games/game-fallout4/src/index.js"
      provides: "Clean source without dead winapi-bindings require"
    - path: "extensions/games/game-fallout4/dist/index.js"
      provides: "Rebuilt dist without dead winapi-bindings require"
  key_links:
    - from: "extensions/games/game-fallout4/src/index.js"
      to: "runtime module loader"
      via: "require() calls at load time"
      pattern: "require\\("
---

<objective>
Audit top-4 game extensions for Linux compatibility and fix Fallout 4 dead winapi-bindings import.

Purpose: STAM-05 confirms that Skyrim SE, Fallout 4, Cyberpunk 2077, and Stardew Valley work on Linux. Skyrim SE and Fallout 4 inherit the STAM-04 INI path fix. Fallout 4 has a dead `require('winapi-bindings')` that causes MODULE_NOT_FOUND on Linux. Cyberpunk and Stardew are confirmed unblocked by research.
Output: Fallout 4 loads on Linux; all 4 titles confirmed manageable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-steam-proton-detection/06-CONTEXT.md
@.planning/phases/06-steam-proton-detection/06-RESEARCH.md
@.planning/phases/06-steam-proton-detection/06-02-SUMMARY.md

<interfaces>
<!-- Files to inspect for audit -->

From extensions/games/game-fallout4/src/index.js (line 4):
```javascript
const winapi = require('winapi-bindings');
// winapi is never referenced after this line in the entire 141-line file
```

From extensions/games/game-cyberpunk2077/src/index.js:
- No winapi-bindings import (confirmed by research)

From extensions/games/game-stardewvalley/src/game/StardewValleyGame.ts:
- Native Linux executable detection (confirmed by research)

From extensions/games/game-skyrimse/:
- Uses {mygames} INI paths; inherits STAM-04 fix via ini_prep/gameSupport.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead winapi-bindings require from Fallout 4 and audit remaining 3 titles</name>
  <files>
    extensions/games/game-fallout4/src/index.js
  </files>
  <read_first>
    extensions/games/game-fallout4/src/index.js
    extensions/games/game-cyberpunk2077/src/index.js
    extensions/games/game-stardewvalley/src/game/StardewValleyGame.ts
    extensions/games/game-skyrimse/src/index.ts
    src/renderer/src/extensions/ini_prep/gameSupport.ts
  </read_first>
  <action>
**Fallout 4 — remove dead import (per D-16):**

In `extensions/games/game-fallout4/src/index.js`, remove line 4:
```javascript
const winapi = require('winapi-bindings');
```

This import is dead code — the `winapi` variable is never referenced anywhere in the file. The webpack alias from Phase 2 does NOT cover bundled game extensions (they use `copyfiles`, not webpack), so this `require()` causes MODULE_NOT_FOUND on Linux at runtime.

After removing the line, rebuild the extension's dist:
```bash
cd extensions/games/game-fallout4 && pnpm run build 2>/dev/null || cp src/index.js dist/index.js
```

(If the extension has no build script, copy src to dist directly. Many JS-only bundled extensions just copy files.)

**Audit — verify the other 3 titles by inspection:**

1. **Cyberpunk 2077** (per D-14): Read `extensions/games/game-cyberpunk2077/src/index.js`. Confirm there is NO `require('winapi-bindings')` or `import ... from 'winapi-bindings'`. Log the confirmation.

2. **Stardew Valley** (per D-15): Read `extensions/games/game-stardewvalley/src/game/StardewValleyGame.ts`. Confirm there is native Linux executable detection (should reference Linux-specific paths or executables). Log the confirmation.

3. **Skyrim SE** (per D-13): Read `extensions/games/game-skyrimse/src/index.ts`. Confirm it uses `{mygames}` template paths that are resolved by `ini_prep/gameSupport.ts` (which now has the STAM-04 Linux guard). Verify there are no additional winapi-bindings imports that would block loading on Linux. Log the confirmation.

For each title, record findings as comments in the SUMMARY.
  </action>
  <verify>
    <automated>cd /home/alex/src/Vortex && grep -c "winapi-bindings" extensions/games/game-fallout4/src/index.js && grep -c "winapi-bindings" extensions/games/game-cyberpunk2077/src/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "winapi-bindings" extensions/games/game-fallout4/src/index.js` returns `0`
    - `grep -c "winapi-bindings" extensions/games/game-cyberpunk2077/src/index.js` returns `0`
    - Fallout 4 index.js still has all other requires (bluebird, path, vortex-api)
    - Fallout 4 dist/index.js also has winapi-bindings removed
    - Stardew Valley has Linux executable path handling
    - Skyrim SE uses {mygames} template paths (inherits STAM-04 fix)
  </acceptance_criteria>
  <done>Fallout 4 dead winapi-bindings import removed; Cyberpunk 2077 confirmed no winapi dependency; Stardew Valley confirmed native Linux support; Skyrim SE confirmed inheriting STAM-04 Wine prefix fix</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Steam/Proton detection end-to-end on Linux</name>
  <what-built>
    Complete Steam/Proton detection pipeline:
    - Multi-root Steam scanning (native + Flatpak dual-install)
    - Proton detection for never-launched games via oslist
    - Correct Wine prefix path for Bethesda INI files (Documents/My Games, not My Documents)
    - Fallout 4 dead winapi import removed
    - All 4 top-4 titles confirmed for Linux compatibility
  </what-built>
  <how-to-verify>
    1. Run `pnpm run build` — must complete without errors
    2. Run `pnpm run start` on Linux
    3. Check that Steam games are detected in the game list
    4. If you have both native and Flatpak Steam installed, verify games from both appear
    5. Select Skyrim SE or Fallout 4 — verify the game settings path points to the Wine prefix (`compatdata/.../steamuser/Documents/My Games/...`) not `~/Documents/My Games/`
    6. Verify Fallout 4 extension loads without MODULE_NOT_FOUND error in the console
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "winapi-bindings" extensions/games/game-fallout4/src/index.js` returns 0
- `pnpm run build` succeeds
- All STAM requirements addressed across plans 01-03
</verification>

<success_criteria>
1. Fallout 4 extension loads on Linux without MODULE_NOT_FOUND
2. Cyberpunk 2077 confirmed no winapi dependency (inspection)
3. Stardew Valley confirmed native Linux support (inspection)
4. Skyrim SE confirmed inheriting Wine prefix INI path fix (inspection)
5. User verifies end-to-end: Steam games detected, INI paths correct, no crashes
</success_criteria>

<output>
After completion, create `.planning/phases/06-steam-proton-detection/06-03-SUMMARY.md`
</output>
