// NOTE(erri120): This file configures prettier, the formatter that used in the entire codebase.
// The config is empty because prettier provides opinionated defaults.
// There is no point in arguing over code styles. Use the defaults.
// For more details: https://prettier.io/docs/option-philosophy

/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {};
export default config;
