// Re-export for backwards compatibility with extensions
// that import from the old MainWindow location
export { MainContext } from "../contexts";
export { AppLayout as MainWindow } from "./AppLayout";
